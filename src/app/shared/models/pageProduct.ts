import {Product} from './product';

export class PageProduct {
  content: Product[];
  totalPages: number;
  totalElements: number;
  last: boolean;
  size: number;
  first: boolean;
  sort: string;
  numberOfElements: number;

  // currentPage,
  // pageSize,
  // startPage,
  // endPage,
  // startIndex,
  // endIndex,
  // pages

}
