import {Order} from './order';

export class User {
  id?: any;
  firstName?: string;
  lastName?: string;
  email?: string;
  phone?: string;
  password?: string;
  checkPassword?: string;
  source?: string;
  comment?: string;
}
